package com.zaus_app.converter.data

import com.zaus_app.converter.data.entity.Valute

class MainRepository {
    val valutes = listOf(
        Valute("1", "ss", "ss", 1, "BY", 1.2, 1.1),
        Valute("2", "bb", "bb", 2, "RU", 12.2, 12.1)
    )
}